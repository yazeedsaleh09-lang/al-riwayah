# Synthetic fixtures

All fixtures in this directory must use invented player labels and artificial room/session data.

They exist for deterministic engine, protocol, screenshot, and E2E tests.

- Never paste real playtest names, answers, recordings, room codes, or tokens.
- Do not use these fixtures in production analytics.
- Regenerate tokens/IDs with test helpers.
- Keep runtime secrets outside Git.

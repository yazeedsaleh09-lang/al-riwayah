packages:
  - "apps/*"
  - "packages/*"
  - "tests"

overrides:
  postcss: 8.5.23
  sharp: 0.35.3

# Native postinstall scripts allowed to run (pnpm blocks these by default).
# esbuild ships a platform binary needed by Vitest and tsx.
allowBuilds:
  esbuild: true
  # sharp is Next's optional image optimizer; unused (we ship inline SVG).
  sharp: false
